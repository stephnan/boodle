(ns boodle.utils.dates-test
  (:require [boodle.utils.dates :as d]
            [clojure.test :refer :all]))

(deftest test-format-date
  (testing "Testing date formatting"
    (let [date (java)]
      (is (= (n/en->ita x) "3,5")))))
